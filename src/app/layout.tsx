import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CompanyBrain AI - Your Company's Second Brain",
  description: "AI-powered knowledge management system that helps you upload, search, and analyze your company documents with intelligent Q&A capabilities.",
  keywords: ["AI", "knowledge management", "document search", "company intelligence", "Elasticsearch", "Gemini AI"],
  authors: [{ name: "CompanyBrain AI Team" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
